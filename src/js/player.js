import items from "items.js";

export default class {
    constructor() {
        this.mothershipCharge = 0;
        this.shapes = [];
    }

    init(data) {
        console.log(data, items);
    }
}